"use strict";

ধ্রুবক { Connection } = require("./lib/connection.bnl");
ধ্রুবক { createPool } = require("./lib/pool.bnl");
ধ্রুবক { escape, escapeId, format } = require("./lib/sql.bnl");

module.exports = {
  createConnection: (opts) => new Connection(opts),
  createPool,
  escape,
  escapeId,
  format,
};